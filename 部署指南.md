---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: 'cc64cf74-e6a9-471f-8327-14d15666ad42'
  PropagateID: 'cc64cf74-e6a9-471f-8327-14d15666ad42'
  ReservedCode1: '04240aa2-e816-40a1-b9b2-7a6730ddb3f4'
  ReservedCode2: '04240aa2-e816-40a1-b9b2-7a6730ddb3f4'
---

# MBTI 性格测评 — GitHub Pages 部署指南

> Git 已安装（v2.47.1），本地仓库已初始化并完成首次提交。
> 你只需完成以下 4 步即可上线。

## 当前状态

- [x] Git 已安装（`C:\Program Files\Git\cmd\git.exe`）
- [x] 本地仓库已建好：`mbti-repo/` 目录
- [x] 网页已重命名为 `index.html`（GitHub Pages 默认首页）
- [x] README.md 已就绪
- [x] 首次提交已完成（commit: `init: MBTI性格测评网页`）
- [ ] **待你操作：创建 GitHub 仓库 → 推送 → 开启 Pages**

---

## 第 1 步：配置你的 Git 身份

打开 PowerShell，执行以下命令（替换为你的 GitHub 信息）：

```powershell
& "C:\Program Files\Git\cmd\git.exe" config --global user.name "你的GitHub用户名"
& "C:\Program Files\Git\cmd\git.exe" config --global user.email "你的GitHub邮箱"
```

## 第 2 步：在 GitHub 创建空仓库

1. 浏览器打开 https://github.com/new
2. Repository name 填 `mbti`
3. 选择 **Public**（GitHub Pages 免费版要求公开仓库）
4. **不要勾选**任何初始化选项（Add README、.gitignore、license 均不选）
5. 点击 Create repository

## 第 3 步：推送到 GitHub

在 PowerShell 中执行（将 `你的用户名` 替换为实际 GitHub 用户名）：

```powershell
cd "C:\Users\zyy\.local\share\TeleAgent\TeleAgent的工作空间\mbti-repo"

& "C:\Program Files\Git\cmd\git.exe" remote add origin https://github.com/你的用户名/mbti.git
& "C:\Program Files\Git\cmd\git.exe" push -u origin main
```

### 认证说明

推送时会弹出 GitHub 登录窗口或要求输入凭据：

- **方式一（推荐）**：浏览器自动弹出 GitHub 登录页，登录后自动完成认证
- **方式二**：如果要求输入密码，需使用 **Personal Access Token（PAT）** 而非账号密码：
  1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
  2. Generate new token → 勾选 `repo` 权限 → 生成并复制令牌
  3. 推送时将令牌粘贴到密码输入框

## 第 4 步：开启 GitHub Pages

1. 进入仓库页面 → **Settings** → **Pages**（左侧边栏）
2. Source 选择 **Deploy from a branch**
3. Branch 选 `main`，目录选 `/ (root)`
4. 点击 **Save**
5. 等待 1~2 分钟，页面顶部显示访问地址：

```
https://你的用户名.github.io/mbti/
```

## 验证

浏览器打开上述地址，确认测评页面正常显示即可。

---

## 后续更新

每次修改网页后，在 `mbti-repo` 目录执行：

```powershell
& "C:\Program Files\Git\cmd\git.exe" add .
& "C:\Program Files\Git\cmd\git.exe" commit -m "更新说明"
& "C:\Program Files\Git\cmd\git.exe" push
```

GitHub Pages 会自动重新发布，约 1 分钟后生效。

## 注意事项

- 仓库必须为 **Public**，GitHub Pages 免费版不支持私有仓库
- 网页文件已命名为 `index.html`，访问 `https://用户名.github.io/mbti/` 即直达测评页
- 纯前端零依赖，无需任何后端配置，完美适配 GitHub Pages 静态托管

> AI生成